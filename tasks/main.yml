---
- name: include php install
  include_tasks: ./tasks/install/php_install__{{ ansible_os_family | lower }}.yml
  when: php_install

- name: include php config
  include_tasks: ./tasks/config/php_config__{{ ansible_os_family | lower }}.yml
  when: php_config